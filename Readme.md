# Browser extension version checker

## Usage

```sh
./version-checker [--table] <edge=...> <chrome=...> <firefox=...>
```

`--table` output is nicer, but blocks rendering until all versions are checked

## Example

![](readme.png)
